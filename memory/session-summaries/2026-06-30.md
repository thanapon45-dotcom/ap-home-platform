# Session Summary — 2026-06-30

## งานที่ทำ

### Fix 1: Vercel proxy 404 (commit a0de659)
- Root cause: HUB_URL env var มี `/api` → double path
- Fix: normalize URL ใน `app/api/blog/queue/run-next/route.ts`
- Result: POST /api/blog/queue/run-next → 200 OK ✅

### Fix 2: /webhook/image-done ใน Hub v2 (commit 856683d)
- WF2 เดิมชี้ Hub v1 URL + ใช้ header ผิด (x-hub-token)
- เพิ่ม endpoint ใน server.ts + type + StateManager.setImageDone()
- อัปเดต WF2 JSON: URL → Hub v2, header → x-hub-secret
- Smoke test: POST /webhook/image-done → {ok:true} ✅

### Documentation (commits 3af2101, 54fb8dd, 37d97a5)
- สร้าง CLAUDE.md, decisions.md, HANDOFF.md, issues-log.md, glossary.md
- สร้าง context_brief.md, อัปเดต AI_TEAM.md v1.2.1
- สร้าง memory/ structure (tokens.md, session-summaries/, n8n-workflows/)

## Bugs ที่เจอ
- Sandbox git add ตัดไฟล์ → ต้องรัน git จาก Windows PowerShell เท่านั้น
- Emoji ใน TypeScript template literal → corrupt (ใช้ plain text แทน)
- Railway ไม่ auto-deploy → ต้อง Redeploy ด้วยตนเอง

## สถานะ Workflows ณ สิ้น session
- Queue Auto-run → Hub v2 via Vercel proxy ✅
- WF1 v8 → Hub v2 /webhook/n8n ✅ (ยืนยัน active ใน n8n)
- WF2 v5 → Hub v2 /webhook/image-done ✅
- Wake-up → Hub v2 /api/health/state ✅

## Pending สำหรับ session ถัดไป
1. QC LINE end-to-end test
2. Verify FB token expiry (Jun 26 vs Aug 2?)
3. WF1 v8 confirm ACTIVE ใน n8n
4. Enable RLS: sites, line_users, qc_inspections, qc_defects, qc_standards, qc_daily_usage
5. REVOKE anon FROM append_line_image_atomic
6. DROP hub_state anon_update policy
7. Blog run run_da089n87 อาจ stuck — ตรวจ GET /api/health/state
